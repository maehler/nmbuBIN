# NMBU Bioinformatics R-package

## Installation

The installation currently requires devtools, so that has to be installed if
not already installed.

```
# install.packages("devtools")
devtools::install_github("maehler/nmbuBIN")
```
